import * as THREE from "three"; class Player { constructor() { this.health = 100; this.ammo = { current: 30, reserve: 90 }; this.team = "teamA"; this.position = new THREE.Vector3(); } takeDamage(amount) { this.health -= amount; if (this.health <= 0) { console.log("Player died"); } } move(direction) { // Movement logic } shoot() { if (this.ammo.current > 0) { this.ammo.current--; return true; } return false; } } export default Player;
